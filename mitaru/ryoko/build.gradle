apply plugin: 'java'

repositories {
    maven {
    	url "http://maven.eclipse.org/nexus/content/repositories/testing/"
    }
}

dependencies {
    compile group: 'org.eclipse.swt', name: 'org.eclipse.swt.win32.win32.x86', version: '3.6.2'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

compileJava {
	
}

task dists(dependsOn:assemble,type:Zip) {
	description = 'Builds the distribution'
	group = 'build'
	from jar.archivePath,files("FFACE.dll","Mitaru.exe","monghul.bat"),configurations.runtime.files
	baseName 'monghul' 
}

println configurations.runtime.files
println jar.archivePath